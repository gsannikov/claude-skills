# Core dependencies
chromadb>=0.4.0
sentence-transformers>=2.2.0
rapidfuzz>=3.0.0
python-dotenv>=1.0.0
requests>=2.28.0

# Document processing
pypdf>=3.0.0
pdf2image>=1.16.0
Pillow>=9.0.0
python-docx>=0.8.11
python-pptx>=0.6.21
openpyxl>=3.0.0

# OCR - PaddleOCR (default, multi-language support including Hebrew)
paddleocr>=2.7.0
paddlepaddle>=2.5.0

# OCR - Surya (alternative, lighter weight)
# surya-ocr>=0.4.0

# Optional: Qdrant vector store (alternative to ChromaDB)
# qdrant-client>=1.7.0

# Optional: Legacy .doc file support
# textract>=1.6.5  # Requires antiword: brew install antiword

# Optional: Cross-encoder reranking (loaded via sentence-transformers)
# No additional package needed - uses sentence-transformers
