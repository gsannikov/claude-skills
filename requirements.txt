# Claude Skills SDK Template - Core Requirements
# These are the base dependencies for the skill template

# ============================================================================
# CORE (Required for all backends)
# ============================================================================

# Configuration file parsing
PyYAML>=6.0

# ============================================================================
# OPTIONAL BACKENDS (Install as needed)
# ============================================================================

# GitHub Backend
# Uncomment if you plan to use GitHub for multi-device storage
# PyGithub>=2.1.1

# Notion Backend
# Uncomment if you plan to use Notion for structured data storage
# notion-client>=2.0.0

# ============================================================================
# INSTALLATION INSTRUCTIONS
# ============================================================================

# Minimal setup (Local Filesystem only):
#   pip install PyYAML

# With GitHub support:
#   pip install PyYAML PyGithub

# With Notion support:
#   pip install PyYAML notion-client

# All optional backends:
#   pip install PyYAML PyGithub notion-client

# Development setup:
#   pip install -r requirements.txt
#   pip install -r requirements-dev.txt

# ============================================================================
# NOTES
# ============================================================================

# - Email backend uses built-in Python libraries (no extra deps)
# - Checkpoint backend has no dependencies
# - Local filesystem backend uses MCP tools (no extra deps)
# - See DEPENDENCIES.md for detailed backend-specific requirements
